export default function Home() {
  //end doorhi logic bairlana
  return (
    <main>
      <p>
        check if user logged in if logged in then go to dashboard if not then go
        to login
      </p>
    </main>
  );
}
